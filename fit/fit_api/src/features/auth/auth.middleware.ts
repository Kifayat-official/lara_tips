export default class AuthMiddleware {
    public static auth() {
        try {
            let token = ""
            if (token) {
                //token = token.split(" ").[0]
            } else {

            }
        } catch (error) {
            console.log(error)
        }
    }
}